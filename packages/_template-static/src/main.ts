const root = document.getElementById('root')
if (root) {
  const h1 = document.createElement('h1')
  h1.textContent = 'Template static — duplica esta carpeta para nuevo proyecto web'
  root.appendChild(h1)
}
