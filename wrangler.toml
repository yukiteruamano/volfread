# Opcional — solo si migras de Pages a Workers Static Assets (SSR)
# Para Pages estático actual NO necesitas este archivo.
# Descomenta y usa `wrangler deploy` si activas `output: 'server'` + @astrojs/cloudflare.
#
# name = "volfread-xyz"
# compatibility_date = "2024-09-23"
# compatibility_flags = ["nodejs_compat"]
#
# [assets]
# directory = "./dist"
# not_found_handling = "single-page-application"
# # Para SPA routing fino, mantén _redirects en public/
